import 'package:flutter/material.dart';

/// Placeholder para o feed atual destinado a barbeiros.
///
/// Em produção este widget deve ser substituído pelo feed real de vagas.
class BarberFeedView extends StatelessWidget {
  const BarberFeedView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Feed de vagas para barbeiros'));
  }
}
