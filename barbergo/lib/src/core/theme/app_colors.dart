import 'package:flutter/material.dart';

/// Paleta de cores oficial do BarberGO — Dark Mode Sofisticado.
class AppColors {
  AppColors._(); // Construtor privado para evitar instâncias.

  /// Cor principal (Lilás).
  static const Color primary = Color(0xFF9B87F5);

  /// Cor de fundo principal (Roxo Escuro).
  static const Color background = Color(0xFF1A1F2C);

  /// Cor de superfície para cards e elementos elevados (slightly lighter).
  static const Color surface = Color(0xFF2A2F3C);

  /// Cor do texto principal (Off‑White) para alto contraste no dark mode.
  static const Color textPrimary = Color(0xFFF5F5F5);

  /// Cor de erro (vermelho).
  static const Color error = Color(0xFFEF5350);
}
